<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
         failOnWarning="true"
         failOnRisky="true">
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>src</directory>
        </include>
    </source>
    <php>
        <env name="APP_ENV" value="testing"/>
        <!-- Testbench's SQLite runs with PRAGMA foreign_keys = 0 by default, so the foreign
             keys on cashier_subscription_items and cashier_invoices would be declared and
             never exercised — cascade, null-on-delete and orphan rejection all unverified,
             on every engine. A tag freezes the schema; this makes the constraints something
             the suite actually defends. -->
        <env name="DB_FOREIGN_KEYS" value="true"/>
    </php>
</phpunit>
