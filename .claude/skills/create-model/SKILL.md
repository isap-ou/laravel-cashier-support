---
description: Create a new abstract Eloquent model in src/Models/
argument-hint: "[ModelName]"
---

# Create Abstract Model

1. `abstract class extends Model` in `src/Models/`
2. Include scopes, relations, status helpers (active, canceled, onTrial, onGracePeriod)