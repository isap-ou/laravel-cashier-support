---
description: Create a new string-backed enum in src/Enums/
argument-hint: "[EnumName]"
---

# Create Enum

1. `enum Name: string` in `src/Enums/`, values in `snake_case`
2. Include `label(): string` method for human-readable display